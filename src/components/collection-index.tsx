'use client';
import {useState} from 'react';
import Link from 'next/link';
import {GarmentImage} from './garment-image';
import {Arrow} from './icons';
const entries=[{name:'Suits',note:'See our suits and their details',href:'/collections/suits',image:'dsc06963',alt:'Fuchsia suit seated on garden steps'},{name:'Wedding wear',note:'For the wedding and its celebrations',href:'/collections/wedding-wear',image:'dsc07051',alt:'Close portrait showing the olive-gold suit and dupatta'},{name:'Lehengas',note:'Tell us the style you have in mind',href:'/collections/lehengas',image:null,alt:''}];
export function CollectionIndex(){const [active,setActive]=useState(0);return <section className="collection-index page-width" aria-labelledby="collection-title"><div className="collection-index-heading"><p className="eyebrow">02 / BROWSE BY STYLE</p><h2 id="collection-title">What are you<br/>looking for?</h2></div><div className="collection-index-layout"><div className="index-preview" aria-hidden="true">{entries[active].image?<GarmentImage id={entries[active].image!} alt="" sizes="40vw"/>:<div className="custom-preview"><span className="eyebrow">CUSTOM LEHENGAS</span><p>Have a lehenga<br/><em>in mind?</em></p><span>Send us a reference to get started.</span></div>}</div><div className="index-entries">{entries.map((item,i)=><Link className={active===i?'index-entry active':'index-entry'} href={item.href} key={item.name} onMouseEnter={()=>setActive(i)} onFocus={()=>setActive(i)}><span className="eyebrow">0{i+1}</span><div><h3>{item.name}</h3><p>{item.note}</p></div><Arrow diagonal/>{item.image&&<GarmentImage className="mobile-index-image" id={item.image} alt={item.alt} sizes="120px"/>}</Link>)}</div></div></section>;}
